"""Exercise 6.1 — Functions working together

Goal
    Split a task into small functions, and have one coordinate the others.

In class
    Write is_even(number) and is_zero(number), then a third function that
    uses both to describe a number.

Improved version
    Add is_positive(number) and is_negative(number). The describing function
    must say both things: whether the number is positive, negative or zero,
    AND whether it is odd or even. Test it on 0, on a positive number and
    on a negative one.

Hints
    A function can call another function.
    One function, one job. The name should say the job.
    Deal with zero first, it is the special case.
    See ../examples/functions/functions_calling_functions.py

Four steps
    Ask, read, adapt, check. Zero is the case people forget.
"""

# TODO 1: is_even, is_zero, and a function that uses them


# TODO 2 (improved version): add is_positive and is_negative, and combine everything
