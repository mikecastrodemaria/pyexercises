"""Exercise 4.0 — Lists

Goal
    Store several values in one place and reach them.

In class
    Build a list of three fruits, add a fourth, then display the second one
    and the whole list.

Improved version
    Build a list of numbers, sort it, display the smallest, the largest and
    the average.

Hints
    .append() adds, [1] reads the second item, .sort() reorders.
    min(), max(), sum() and len() do the rest.
    See ../examples/lists/list_methods.py

Four steps
    Ask, read, adapt, check. Compute the average by hand once, and compare.
"""

# TODO 1: create a list, add an item, display one item and the whole list


# TODO 2 (improved version): sort a list of numbers and describe it
