"""Exercise 2.0 — User input and string concatenation

Goal
    Ask the user something, then use the answer.

In class
    Ask for the user's name, then greet them by name.

Improved version
    Ask for the name AND the age, then print a sentence containing both.
    Remember that input() gives you text, so the age must be converted
    before you can compute with it.

Hints
    input("question ") displays the question and waits.
    int("30") turns text into a number.
    See ../examples/io/input_function.py

Four steps
    Ask, read, adapt, check.
"""

# TODO 1: ask for the name and greet the user


# TODO 2 (improved version): also ask for the age, and use it in a sentence
